# 真机首启检查清单（SF32LB52-DevKit-LCD）

> 目标：板子到手后，按本清单 30 分钟内跑通「烧录 → 三项验证 → 留证据」。
> 这是「预备上真机」的可执行清单。带 `[待SDK确认]` 的是烧录工具/命令，以
> vendor_sifli 实际 SDK 为准，其余步骤已按 openvela 标准流程定死。

---

## 一、板子到手当天（约 09/12）

### 1. 开箱清点
- [ ] 开发板本体、1.85" AMOLED 屏、MEMS MIC、Class-D 扬声器/耳机座
- [ ] Type-C 数据线（供电 + 调试）
- [ ] 调试串口线（若有独立串口）；板载无 IMU/马达/环境光（如实，别装进方案）

### 2. 实拍（当天就拍，是「作品展示照片」素材）
- [ ] 前视图 / 后视图 / 侧视图 / 俯视图 各一张（多角度）
- [ ] 通电点亮 AMOLED 的正面照
- [ ] 存到 `outputs/`，命名 `作品照片_<角度>.jpg`

---

## 二、烧录 openvela 固件

### 1. 编译环境（Ubuntu 22.04，租云或 VM）
- [ ] Ubuntu 22.04、8C16G、系统盘 ≥100G
- [ ] 装依赖：`repo`、`git`、`gcc`、`python3`、`cmake`、`ninja` 等（`build.sh` 会校验）

### 2. 拉工程 + 编译
- [ ] `repo init -u <组委会专属仓> -b dev-ai-contest-2026 -m contest2026_<编号>_<队名>.xml`
- [ ] `repo sync -c -j8`
- [ ] 把 `mianyu/` 源码放进 `app/<你的应用>/`，menuconfig 启用 AI Agent + LVGL + 本应用
- [ ] `build.sh vendor/openvela/boards/vela/configs/<板型>/ --cmake -j4`
- [ ] **编译通过 = 排掉「代码进不了工程」的雷**

### 3. 烧录到板子 `[待SDK确认]`
- [ ] 用 vendor_sifli 提供的烧录工具（下载器/串口 ISP）写入固件
- [ ] 上电，观察启动日志（**保存串口日志** → `logs/` 或 `docs/`）
- [ ] 首次启动验证唤醒词「你好，openvela / Hello，openvela」

---

## 三、三项验证（真机证据核心）

| 验证项 | 操作 | 通过标准 | 证据 |
|---|---|---|---|
| ① 音频录放链路 | 触发睡前流程，播端侧合成棕噪 | 扬声器出声、无爆音 | 录屏/录音 |
| ② MIC 实采 | 串口打印 MIC 帧能量/波形 | 有稳定呼吸节律包络 | 串口日志 |
| ③ AMOLED 暗光 | 呼吸光晕上屏，关灯 | 纯黑底 + 暖光，零光污染 | 暗光照片 |

- [ ] 三项都过 → 核心闭环在真机成立，可录演示视频
- [ ] 任一项异常 → 记录到 `docs/测试记录表.md`，当天排查（不隔夜）

---

## 四、真机标定（09/13 首晚）

- [ ] 整夜录音（成年人本人，样本只用成年人）
- [ ] 人工标注「入睡/清醒/夜醒」真值
- [ ] 录音回放喂 `my_sleep_feed_mic`，标定 `regularity_enter/asleep/wake` 阈值
- [ ] 数据当天记入 `docs/测试记录表.md` 表 2（不隔夜）

**兜底**：若板未到或标定失败，用模拟器 Mock 数据 + 报告如实标注「模拟器估计值」，官方口径不扣分。

---

## 五、提交证据清单（9/20 前集齐）

| 证据 | 来源 | 去向 |
|---|---|---|
| 实拍照片（多角度） | 一.2 | 提交压缩包「作品展示照片」 |
| 串口日志 | 二.3 / 三.② | 仓库 `docs/` 或 `logs/` |
| 真机录屏 | 三 + 四 | 演示视频素材 |
| 混淆矩阵真机数据 | 四 | 技术报告 3.5 |

---

## 六、剩余时间线

| 期限 | 事项 |
|---|---|
| 09/12 | 板到手：开箱 + 实拍 + 烧录 + 三项验证 |
| 09/13 | 首晚标定（兜底：模拟器 Mock） |
| 09/13 前 | 系统框图 + 数据流图（✅ 已就绪 `docs/系统框图与数据流图.html`） |
| 09/16–09/18 | 演示视频录制，录完本地先完整播一遍再提交 |
| 09/20 前 | 提交：源码仓 + 技术报告 + 演示视频 + AI Coding 日志 |

---

## 阻塞项（需要用户行动，非我能代）

1. **板型确认**（最急，24h 内群里问）：报名表黄山派 vs 补交 DevKit-LCD，按实际发的板调整方案。
2. **交押金 50 元**（备注 `小鸣TV-开发板押金-<板型>`）。
3. **官方专属仓**：等 GitHub 邀请 → `git remote add origin` + push 本地分支。
4. **作品名最终确认**：HANDOVER 说「安眠科技」，技术报告 docx 信息表仍写「眠语」，两者须一致。
5. **Ubuntu 编译环境**：租云或 VM（编译固件必需，与上不上真机无关）。
